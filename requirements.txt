# Core (JP4 / Py3.6 safe)
numpy==1.19.4
opencv-python==4.5.5.64
loguru==0.7.2
python-dotenv==0.20.0
requests==2.27.1
# python-snap7==1.3
pydantic==1.9.2

# mediapipe==0.8.6

# Note:
# Do NOT install ultralytics / torch>=2 / torchvision>=0.16 on JP4
# torch is already installed on the system with CUDA=True
